pragma solidity >= 0.7.0 < 0.9.0;

contract mappings{
    //mapping accept key and value 
    //key can be uint or bool  and value can be anything
    //then assign a name like myMap or anything

    mapping(address => uint) public myMap; 
}